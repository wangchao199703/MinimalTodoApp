# v2.0.0

整体重写:C#/WPF → Tauri v2(Rust + SQLite + React 19)。

- 包体 63 MB → 约 10 MB,启动更快,数据改动即时落盘(SQLite WAL)
- 全部功能对齐旧版:任务/子任务/优先级/截止倒计时/周期提醒/置顶/排序、
  四象限、标签看板、便签(Markdown)、日程月历与国内节假日、
  102 套主题 + 自定义主题、中英双语、托盘/贴边停靠/置顶/开机自启、自动更新
- 首次启动自动导入旧版 data.json(任务/标签/便签/自定义主题/全部设置),原文件保留
- 旧版如需回退:数据文件未做破坏性改动,直接运行旧 exe 即可
