# 交接文档 —— MinimalTodoApp v2(Tauri 重写版)

> 写给接手的 Claude:开工前先读完本文 + 根目录 `CLAUDE.md`。旧版功能迁移已基本完成,
> 现进入「对齐旧版细节 + 按用户反馈打磨」阶段。
> 最后更新:2026-06-13(第二日,多轮打磨后)。

---

## 一、背景:这个项目是什么

- 原版是 C#/WPF + .NET 8 的 Windows 本地待办应用(单文件 exe 63MB),**完整保留在 `legacy/` 目录**,是一切迁移工作的参照物。
- 2026-06-12 起按用户指定技术栈整体重写:**Tauri v2 + Rust(rusqlite/SQLite WAL)+ React 19 + TypeScript + Zustand v5(无路由)+ Tailwind v4 + pragmatic-drag-and-drop + auto-animate**。包体约 10MB。
- 重写按里程碑 M1~M8 完成了主体(工程/任务/导入/主题/外壳/便签日历/更新/文档),之后又按用户逐轮反馈大量打磨(主题改版三次、瀑布流看板、日历三视图、贴边停靠对齐旧版等)。全部历史见 `优化记录.md` 与 git log。
- 架构、命令、约定、踩坑(time crate 钉 0.3.47、vite8 配套、IPC 三态约定等)都在 **`CLAUDE.md`**,不重复。

## 二、必须遵守的两个记录文档(用户的硬性仪式)

1. **`优化记录.md`(项目根)** — 每轮需求做完后追加记录,**必须包含用户原始提示词逐字**、改动摘要、commit hash。格式照现有条目。
2. **`release.md`(项目根)** — 发版时记版本号 + 变更摘要(最新在上)。版本号需三处同步递增:`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`package.json`;面向用户的双语说明另写根 `release-notes.md`。

其他每轮规矩:i18n 新键必须 zh/en 双语同步(`src/lib/i18n.ts` 的 EXTRA 或 `src/i18n/*.json`);`npm run build` 必须过;改 Rust 跑 `cargo check`;中文 commit;**默认只本地提交,不 push、不跑 release.ps1,除非用户明说**。

## 三、当前状态(2026-06-13 第二日)

- 分支 main,**所有提交均未推送远端**(远端仍是旧 WPF 版,等用户决定何时推)。
- v2.0.0 尚未发布过 Release;`release.ps1` 已重写但未实跑。版本号一直是 2.0.0,本阶段未递增。
- 开发:`npm run tauri dev`(`cargo` 不在默认 PATH,需先 `$env:Path = "$env:USERPROFILE\.cargo\bin;$env:Path"`);改 Rust 文件 dev 会自动重编重启,改 tauri.conf.json 需手动完全重启;端口/进程残留 `taskkill /F /IM minimal-todo.exe`。
- 数据库 `%AppData%\MinimalTodoApp\todo.db`;已自动导入用户真实数据。
- 桌面有一份手动构建的便携 exe(`npx tauri build --no-bundle` → 复制到桌面,10MB,FileVersion 2.0.0),是用户验收用的,**不是 Release**。
- UI 自动化验证脚本:`%TEMP%\app-ui-helpers.ps1`(Focus-App/Click-At/RightClick-At/Shot 等,逻辑坐标×1.5)。坑:① 用户常同时操作电脑,合成鼠标拖拽不可靠,关键交互让用户手测;② 终端窗口偶尔盖住应用,截图前先 Focus-App;③ 主题下拉项多到要滚动、合成滚轮不稳——**预览某主题最稳的办法是直接写 DB 再重启**:`python -X utf8 %TEMP%\settheme.py <主题键>`(脚本已在,改 settings.theme)。

### ⚠️ 有一轮工作「已写代码、未提交」——接手第一件事先收尾它
- 用户说「当前主题不好看,设计几套耐看经典的」,已新增 **4 套经典护眼主题**:浅色 **Solarized**(`light-solarized`)/ **Paper** 羊皮纸(`light-paper`),深色 **Solarized**(`dark-solarized`)/ **Gruvbox**(`dark-gruvbox`)。均用各自官方原始色值。
- 改动文件(`git status` 可见未提交):`src/lib/themes.ts`(数组/LABELS/PREVIEW 三处)、`src/index.css`(4 个变体块)、`src/components/TitleBar.tsx`(THEME_OPTIONS 图标 Sunrise/BookOpen/Eclipse/Box + import)。
- 状态:`npm run build` **已过**;Solarized 明暗 + Paper 已实测截图 OK,Gruvbox 刚切到正在预览。**待办**:① 追加 `优化记录.md`(用户原话见上);② 中文 commit。无 Rust 改动,不需 cargo check。
- **副作用**:预览时把 DB 的 `settings.theme` 改成了 `dark-gruvbox`,用户下次打开是 Gruvbox 深色——无害,UI 里点主题图标即可换;若用户想要别的默认,改回去即可。
- 后台可能还有一个 `npm run tauri dev` 在跑(预览 Gruvbox 用),接手前可 `taskkill /F /IM minimal-todo.exe` 清掉重起。

## 四、进展总览(2026-06-13 第二日,本日多轮打磨)

旧版功能迁移**已基本完成**。本日提交链(均未 push,新→旧):
- `e61e34f` 收集箱实体化为普通便签分组(初始自带、可删可改名;Rust 自愈逻辑 ensure_notes_grouped)
- `69547a3` 便签二级分组对齐一级行版式(按钮悬停淡入 + 整行点击折叠 + 左对齐)
- `f7e2c3f` 便签第二侧边栏合并进主侧栏(新建 `NotesTree.tsx`,悬停淡入折叠/新建)
- `25697fd` 全局细长滚动条(对齐旧版 ScrollThumb,`::-webkit-scrollbar`)
- `bf4d98d` 便签工具栏窄窗口换行
- `2bc4b8a` 侧栏标签行对齐 NavRow(折叠/新建悬停淡入 + 箭头旋转)
- `7840570` **大轮**:WPF 动画体系移植(index.css 的 modal-in/pop-in/view-in/cal-flip 等)+ 便签 tiptap 实时 Markdown/插图 + 补齐缺失功能(快捷提醒/帮助/导入导出/便签拖拽/完成音效/托盘随语言重建)+ 实测修 3 bug(含存量 Tauri 参数 camelCase 致 create_note 静默失效)
- `7e3f269` 贴边隐藏抄旧版动画(220ms CubicEase 滑出/滑入)
- `54ce44c` 高优先级四项迁移(TaskEditDialog/ConfirmDialog/标签改色改图标/清空已完成)
- 另有未提交的「4 套经典主题」一轮(见上 §三 ⚠️)。
- 完整逐轮记录(含用户原话)见 `优化记录.md` 第六、七节。

> 注意:`CLAUDE.md` 已补两个新坑——Tauri 多词裸参数要 `#[tauri::command(rename_all = "snake_case")]`;Tailwind v4 的 `-translate-*` 是独立 CSS 属性,keyframes 别再写 transform:translate 否则双重位移。动手前务必看。

### 旧版功能迁移清单(对照存档,绝大多数 ✅)

参照物全在 `legacy/MinimalTodoApp/`。

### 高优先级(日常使用明显缺失)—— ✅ 已全部完成(commit 54ce44c,2026-06-13)
1. ~~**任务编辑对话框**~~ ✅ `src/components/dialogs/TaskEditDialog.tsx`,右键任务→「编辑」打开(标题/优先级/截止时分)。
2. ~~**删除确认**~~ ✅ `src/components/ui/ConfirmDialog.tsx` 全局 confirm(),删任务/标签/便签/便签分组均接入。
3. ~~**标签改颜色/图标**~~ ✅ 侧栏与标签看板右键菜单 → TagColorDialog(12 预设+取色器)/IconPickerDialog(六分类 Segoe 字形);**「自定义图片导入」未做**(需 dialog 插件/资源协议,见低优先级)。
4. ~~**清空已完成**~~ ✅ 已完成视图标题栏按钮 + 确认。

### 中优先级 —— ✅ 已全部完成(2026-06-13 第二轮)
5. ~~**新任务快捷提醒**~~ ✅ QuickAdd 铃铛 + ReminderPicker(快捷档/自定义值×单位)。
6. ~~**便签增强**~~ ✅ 全面升级为 tiptap 实时 Markdown 编辑器(输入语法实时生效,旧版没有的新能力):图片粘贴/拖入/工具栏插入(存 note-images + asset 协议渲染)、`<color=#xxx>` 实时着色兼容、`<img=fn>` 兼容、便签拖拽排序/拖入分组。
7. ~~**帮助对话框**~~ ✅ `dialogs/HelpDialog.tsx`(☰ 菜单 → 使用说明)。
8. ~~**导入/导出**~~ ✅ Markdown 导入导出(对齐旧版 MarkdownService 格式;☰ 菜单 → 导入导出;导出写桌面)。

### 低优先级 / 仍可做(旧版有、新版尚缺)
- **标签图标「自定义图片导入」**(`Infrastructure/GroupIcons.cs` 的 ImportImage/CustomImages):asset 协议已开通(便签图片在用),可仿 `NoteEditor.tsx` 的 `<input type="file">`→Rust 写 `group-icons` 目录;`groups.icon_image` 字段 DB/IPC 链路已通,只差 UI。
- **提醒 Toast 行为对齐**(`Views/ToastWindow.xaml`):旧版气泡在左下角、可点击聚焦任务;现为应用内顶部 Toast(差异小,问用户是否要)。
- **单实例接管提示**:旧版二开时弹框征询;现直接唤起已有实例(可能够用,问用户)。
- ~~完成音效~~ ✅ / ~~托盘随语言重建~~ ✅ 已做。

### 已经超出旧版的新能力(本重写新增,旧版没有)
- 便签 **tiptap 所见即所得 + 实时 Markdown + 插图**(旧版只有富文本)。
- 收集箱实体化为普通分组。
- 4 套经典主题(Solarized/Paper/Gruvbox,待提交)。

> 用户最新意图:**「继续基于 wpf 构建新应用」**——即继续以 legacy 为参照打磨/补功能。没有明确新需求时,默认按用户逐轮反馈走,口头禅「参考一下之前的 wpf」。

### 已知技术债
- `src/lib/themes.ts` 的 `isGlassTheme`、Rust 端 `set_acrylic`/`get_custom_themes` 等命令与 custom_themes 表为旧主题方案遗留,现 UI 未使用(保留无害,可问用户是否要恢复自定义主题编辑器)。
- 日历打开时窗口右扩逻辑(`src/App.tsx` 的 scheduleOpen effect)与贴边停靠存在潜在竞态(旧版注释专门提过),如出怪 bug 先查这两处交互。
- 自动更新检查在用户网络下连不通 GitHub(api.github.com),行为是优雅降级提示;发布链路(release.ps1)未实测。

## 五、动手前 checklist

1. **先收尾未提交的主题轮**(§三 ⚠️):追加 `优化记录.md`(用户原话「当前的主题不好看,你给我设计几套主题,要求耐看,经典」)→ 中文 commit `themes.ts`/`index.css`/`TitleBar.tsx`。`npm run build` 已过,直接提交即可。
2. 读 `CLAUDE.md`(架构/命令/坑,尤其新补的两条)。
3. `npm run tauri dev` 跑起来(记得先把 cargo 加进 PATH),确认应用正常;必要时 `taskkill /F /IM minimal-todo.exe` 清残留。
4. 做任何一项时先读 legacy 对应源码,**对齐旧版行为**是默认要求(用户口头禅:"参考一下之前的 wpf")。
5. 每轮结束:`npm run build`(+ 改 Rust 才 cargo check)过 → 中文 commit(带 `Co-Authored-By: Claude ...`)→ 记 `优化记录.md`(**用户原话逐字!**)→ **不 push、不跑 release.ps1**,除非用户明说。
