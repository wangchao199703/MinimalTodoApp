# 原始提示词记录(prompts_origin.md)

> 用户每条需求的**原始原文** + 时间戳(不做提炼;敏感信息已剔除)。提炼/落地见 `prompts.md`。

---

## 2026-06-23(v2.0.4 批次)

1. 将下面更新到当前项目的 claude.md(13 条工作约定:称呼主人;做完先 run 再记文档;prompts_origin.md 记原文+时间戳 / prompts.md 记提炼+落地+时间戳;release.md 记改动+时间+版本号,不擅自升版本;做完即 commit 不 push;handoff.md 交接;不假设用户清楚需求、有疑问停下问;路径非最短直接建议;追根因不打补丁;输出说重点;GitHub token 在桌面 tk.txt 优先 token 推送;软件默认装 D:\BaiduSyncdisk\app\${软件名称};Clean Code 规范四条)。

2. 当前便签右键显示并居中,和单击的逻辑要区分开;单击的时候,如果是隐藏的,就取消隐藏,但是不要居中;如果没有隐藏,就显示到最上层,不要改变待办的位置和大小。

3. 版本号为 2.0.4。

4. 新建待办的优先级和标签一样,弹出一个上拉框,有高中低 3 个可选,风格保持统一。

5. 给输入待办的弹出上拉框加个标题,所有的都加,表名(标明)当前弹窗是什么的选项。

6. 将当前的周期提醒和截至日期的上拉框的逻辑描述一下,我让 gemini 优化一下。

7. (粘贴 Gemini 的优化方案,要点)按这样优化截至日期和周期提醒上拉框:DuePicker——分钟改 5 分钟步长(12 项)+ 精准开关;快捷区加「今晚/明早」定点时间;选完日期自动聚焦小时。ReminderPicker——12 档分「短/常规/长」三段显示、选中 bg-accent 高亮 + 过渡;自定义区加「将每 X 提醒一次」人话实时预览、input+select 做成胶囊整体。

8. 截至时间移除定点,参考周期提醒,改为短周期、常规、长周期。

9. run 起来。

10. 待办在隐藏的时候,我点击边缘取消隐藏,然后我修改待办的时间,待办自己又隐藏了,在我进行编辑的时候不要取消隐藏(不要收起),在我编辑结束后鼠标移开再触发收起。

11. 对便签的分组右键新增重命名,对便签右键新增重命名以及双击便签重命名,参考双击分组重命名。

12. 标题改了没有同步到标题栏,又被第一行自动刷新了。

13. 已完成和四象限在侧边栏没有数字显示,统一一下,都加上数字显示。

14. 所有待办的条数只统计父任务数,和四象限标签的逻辑统一。

15. 已完成也只统计父任务数。

16. 标签和所有待办一样,显示的数量为所有待办的数量。

17. 剪切板的图片预览功能之前是正常的,后面加了抬起动画就不正常了,现在修复了,但是部分电脑还是预览不了,部分电脑可以预览,这块再修复一下。

18. D:\BaiduSyncdisk\code\ShellPicker\Ditto 参考一下这个,看一下他的实现逻辑,双击剪切项,会自动粘贴到之前光标的位置。(随后:按照上面的方案实现;有开关,默认都打开这个功能。)

19. (自动粘贴边界)目标若是管理员权限窗口(UAC),普通权限发键可能被系统拦(Ditto 用单独提权进程处理,本版暂未做);极少数 app 用 Shift+Insert 而非 Ctrl+V;这几个点适配一下。

20. 记录文档并且 release。
