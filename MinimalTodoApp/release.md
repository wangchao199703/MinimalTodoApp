# MinimalTodoApp 版本发布记录

> 每次发布在此登记版本号与变更摘要（最新在上）。详细实现见同目录 `优化记录.md`，对外发布说明见仓库根 `release-notes.md`。

### v1.2.1（新增四象限视图）—— 2026-06-12（版本号保持 **1.2.1**，**覆盖发布 v1.2.1 Release**）

版本号保持 **1.2.1**（`<Version>1.2.1` / `<FileVersion>1.2.1.0` / `<AssemblyVersion>1.2.1.0`，未改动）：

1. **四象限（艾森豪威尔矩阵）视图**：侧栏「已完成」**正下方、同层级**新增「四象限」入口；按**优先级（重要）+ 截止日期（紧急）**把顶层未完成待办自动分到 Q1 立即处理 / Q2 计划安排 / Q3 委派他人 / Q4 考虑删除 四格。
2. **可调判定规则**：设置→待办新增两个开关——「重要含『中』优先级」（默认开）、「紧急含『3 天内到期』」（默认关），切换后实时重新归类。
3. **拖拽**：同象限内拖动重排；跨象限拖动让卡片**停在目标象限且不改变其优先级/截止日期**（用 `TodoItem.QuadrantOverride` 记忆；手动改优先级/截止后自动回归自动归类）。
4. **只展示顶层待办**：子待办不单独进矩阵；矩阵内勾选完成有「卡片左滑消失 + 下方顶上来」动画（与列表一致）+ 烟花。
5. **矩阵内可新建**：四象限保留底部输入栏（含父待办下拉），新建任务落为「未分组」。
6. **「已完成」右键清空**：「已完成」分组右键菜单新增「清空已完成」，一键删除全部已完成任务。
7. **国际化**：新增 `S.Group.Quadrant`、`S.Quadrant.Q1~Q4Title/Desc`、`S.Settings.QuadrantImportant(+Desc)`、`S.Settings.QuadrantUrgent(+Desc)`、`S.Group.ClearCompleted`，中英成对、无裸键。

> 本轮按用户要求以**覆盖 v1.2.1** 方式发布：移动 v1.2.1 tag 至新提交并经 `release.ps1` 重发 Release（替换无依赖单文件 exe 资产）。详细实现见 `优化记录.md` 第四十二轮。

### v1.2.1（设置拆分 + 切换动画 + 恢复默认增强 + 侧栏色块重写）—— 2026-06-11（**版本号升至 1.2.1，正式发布 v1.2.1 Release**）

版本号升级为 **1.2.1**（`<Version>1.2.1` / `<FileVersion>1.2.1.0` / `<AssemblyVersion>1.2.1.0`，三处同步）：

1. **设置拆分（待办 / 收集箱）**：设置窗口导航重排为 **通用 / 待办 / 收集箱 / 关于** 四项。
   - 「待办」页 = 任务区字体（字体/字号/行距/勾选框）+ 待办开关（完成特效/完成音效/提醒音效/显示节假日/优先级色块）；
   - 「收集箱」页 = 便签**专属排版**（便签字体/字号/行距，与任务区独立）；新增 `AppData.NoteFontFamily/NoteFontSize/NoteLineSpacing`，**默认继承全局**，存量便签外观不变；
   - 「通用」页 = 开机自启动 + 恢复默认设置。
2. **切换动画补全**：新增 `MainViewModel.CentralViewAnimate` 事件，待办↔待办、便签↔便签 切换也播放 `IntroScaleFade`（与待办↔便签一致），无重复动画。
3. **恢复默认增强**：`ResetDefaultSettings` 扩展为重置 外观（主题）+ 全部行为开关 + 布局尺寸 + 待办与便签排版；**不动**语言与便签/分组/任务等数据；设置窗口重置后回填勾选框。
4. **侧栏选中色块重写（单一数据源）**：废弃多 ListBox 各自 `IsSelected` 的高亮方式，改由唯一入口 `MainViewModel.RefreshSidebarSelection()` 统一计算 `TodoGroup.IsHighlighted` / `Note.IsActive` / `NoteGroup.HasActiveNote`，全侧栏**任何时刻恰好一个色块**，展开与折叠共用。修复：跨分组选不中第二篇便签；「已完成」/便签在某些状态无色块；窄条选中态补上**强调色竖条 + 底色**（与展开列表一致，淡底色在 40px 窄条几乎不可见的问题解决）；选中便签所在分组/收集箱根折叠时色块落到分组头/文件夹/收集箱图标兜底。
5. **国际化**：新增 `S.Settings.Nav.Todo/Nav.Inbox/NoteFont/NoteFontDesc`、改 `S.Settings.RestoreDefaultsDesc/RestoreDone`，中英成对无裸键。

> 本轮经 `release.ps1` 正式发布 **v1.2.1** Release（打 tag v1.2.1、上传无依赖单文件 exe）。详细实现见 `优化记录.md` 第四十一轮。

### v1.2.0 补7·微调（侧栏层级对齐 + 折叠/重命名/滚动条打磨）—— 2026-06-11（版本号保持 1.2.0，**重新发布 v1.2.0 Release**）

版本号保持 **1.2.0**（`<Version>1.2.0` / `<FileVersion>1.2.0.0` / `<AssemblyVersion>1.2.0.0`，未改动）：

1. **侧栏三级层级对齐**：所有待办 / 已完成 / 收集箱三者图标同一竖线对齐；待办分组、便签分组、未分组便签同为第二层；分组内便签为第三层，逐级缩进。
2. **「所有待办」折叠箭头**：与收集箱一致新增可见折叠箭头，点击收起/展开其下分组（普通分组不加）；同时修正折叠箭头未居中、收起态箭头偏上的问题。
3. **折叠态收集箱可定位**：收起侧边栏后，收集箱化为与待办分组一致的可定位色块，点击=选中收集箱视图（进入便签），当前在便签视图时高亮。
4. **便签重命名交互**：去掉显示异常的悬停「重命名」铅笔按钮，改为**双击**或**右键**重命名。
5. **滚动条美化**：更细（8→6）、更浅（thumb 透明度下调）、圆角微调，侧栏与主列表一致。
6. **折叠态收集箱图标列**：收起侧边后，收集箱整树化为图标列（收集箱 + 便签分组 + 各便签），与待办分组一致；点便签图标直接打开该篇、选中便签有色块高亮；折叠态已完成图标修正裁切/居中。
7. **图标重做**：收集箱 / 分组 / 便签三枚图标改为大小统一、风格统一、各不相同（收纳箱 / 文件夹 / 便签）。
8. **滚动条细化 + 不遮挡 + 上下对齐**：根因是外层 `ScrollViewer` 把滚动条宽度写死、且占布局宽度，导致窄条图标被挤偏遮挡、与底部按钮不对齐。改用自建**浮层滚动条**（不占布局宽度、本地控制粗细），滚动区与底部按钮按完整宽度居中对齐，滚动条只在最右缘浮一条极细线。
9. **国际化**：无新增/改动本地化键（纯布局与交互改动），中英成对无裸键。

> 本轮版本号保持 v1.2.0，推送 GitHub 并**重新发布 v1.2.0 Release**（替换资产，版本号不变）。详细实现见 `优化记录.md` 第四十轮·补4。

### v1.2.0 补6·微调（重做前置优先级色块为整条左脊，重新发布 v1.2.0）—— 2026-06-11

版本号保持 **1.2.0**（`<Version>1.2.0` / `<FileVersion>1.2.0.0` / `<AssemblyVersion>1.2.0.0`，未改动）：

1. **前置优先级色块重做为「整条左脊」**：由原先的短竖条改为贴卡片**左边缘、随圆角收口的满高淡色竖条**（4px 宽，红/橙/绿按优先级）。实现为卡片模板最上层的覆盖层 + `RoundedClip` 附加属性按尺寸裁 12 圆角；颜色用新增 `PriorityToLightBrushConverter` 在优先级色基础上向白混合 45% 调浅。
2. **默认不勾选、不影响存量用户**：`ShowPriorityBlock` 仍默认关闭；本轮**不修改任何用户默认配置**（卡片内边距、任务行间距、默认勾选框直径均保持原值）。
3. **国际化**：无新增/改动本地化键（纯视觉改动，沿用 `S.Settings.PriorityBlock`）。

> 本轮经 `release.ps1` 重新发布 v1.2.0 Release 包。详细实现见 `优化记录.md` 第四十轮·补3。

### v1.2.0 补5·微调（重新发布 v1.2.0）—— 2026-06-10

在补5基础上的微调，重新发布 v1.2.0 Release（版本号不变）：

1. **优先级色块靠左、与卡片边缘平齐**：色块移到任务卡片左边缘的竖条（纵向居中，避开圆角），不再排在勾选框前面。
2. **便签支持拖入图片 / 粘贴图片**：图片文件可直接拖入便签，截图等剪贴板图片可 Ctrl+V 粘贴，均复制持久化到本地 `note-images` 并内嵌。
3. **便签可重命名**：默认「无标题便签」可重命名——鼠标移到便签上点行尾铅笔、或右键「重命名」即可内联改名（自定义标题持久化，优先于正文首行显示）。

---

### v1.2.0 补5（第四十轮）—— 2026-06-10（版本号保持 1.2.0，**本轮发布 Release 包**）

便签字体统一 + 便签插图 + 待办优先级色块。版本号保持 **1.2.0**（`<Version>1.2.0` / `<FileVersion>1.2.0.0` / `<AssemblyVersion>1.2.0.0`，未改动）：

1. **便签字体字号改用全局**：移除上一轮便签独立的「收集箱」字体/字号/行距设置与编辑器字号按钮——便签正文统一跟随设置「字体」页的全局字体/字号/行距（与任务区一致）。保留便签选区着色。
2. **便签插入图片**：工具栏新增「插入图片」，选图后复制持久化到 `%AppData%\MinimalTodoApp\note-images`，正文以 `<img=文件名>` 记录、重开自动还原。
3. **待办优先级色块**：设置「常规」新增开关——开启后勾选圈不显示优先级颜色（转中性灰），改在任务最前方用红/黄/绿色块区分优先级（类似分组色块）。
4. **数据兼容**：新增 `AppData.ShowPriorityBlock`（默认关），移除上一轮 `NoteFontFamily/NoteFontSize/NoteLineSpacing`，旧 data.json 完全兼容。**国际化**：新增 `S.Note.InsertImage` / `S.Settings.PriorityBlock(Desc)`，移除 `S.Settings.Nav.Inbox`/`S.Settings.Note.*`，中英成对。

> 本轮经 `release.ps1` 发布 Release 包。详细实现见 `优化记录.md` 第四十轮。

### v1.2.0 补4（第三十九轮）—— 2026-06-10（版本号保持 1.2.0）

分组与收集箱重构 + 便签富文本增强（**本轮仅构建无依赖 exe，暂不发布 Release 包**）。版本号保持 **1.2.0**（`<Version>1.2.0` / `<FileVersion>1.2.0.0` / `<AssemblyVersion>1.2.0.0`，未改动）：

1. **分组右键化**：移除独立「新建分组」按钮——右键任意分组即可「新建分组」（建后内联重命名）；普通分组右键「重命名」；「所有待办」右键「折叠/展开」收起其下普通分组（`TodoGroup.IsCollapsed`）。
2. **未分组待办**：在「所有待办」里新建的待办归属聚合组本身、不进任何普通分组，可拖到「所有待办」以移出分组。
3. **收集箱重构为可折叠便签树**：支持右键「新建分组」、便签拖拽自由排序 / 跨组归组 / 拖回根；收集箱根与各便签分组均可右键折叠/展开；「写点什么」移到收集箱**最底部**；收起侧栏时收集箱显示为图标按钮。
4. **便签富文本**：选区可改**字体颜色 / 字号**并持久化（`Note.Content` 内扩展 `<color=#RRGGBB>` / `<size=N>` 标记双向解析）；设置新增**「收集箱」页**，可设新建便签的默认字体 / 字号 / 行距。
5. **选中文本→右键「加入到待办」**：便签里选中文本可逐行加入待办。
6. **现代化删除弹窗**：新增主题化 `ConfirmDialog` 替代系统 `MessageBox`（便签 / 便签分组删除复用）。
7. **修复**：鼠标悬停任务卡片时文字发虚——阴影改挂独立背景层，文字不再被位图特效模糊。
8. **数据兼容**：新增 `NoteGroup` 模型与 `AppData` 5 个带默认值字段（`NoteGroups`/`InboxCollapsed`/`NoteFontFamily`/`NoteFontSize`/`NoteLineSpacing`），旧 data.json 完全兼容。**国际化**：新增 `S.Group.*` / `S.Note.*` / `S.Menu.*` / `S.Settings.Note.*` 等键，中英成对。

> 详细实现见 `优化记录.md` 第三十九轮。

## v1.2.0（第三十七轮）—— 2026-06-10

版本号升至 **1.2.0**（`<Version>1.2.0` / `<FileVersion>1.2.0.0` / `<AssemblyVersion>1.2.0.0`）。新增「便签」模块（**本轮仅构建无依赖 exe，暂不发布 Release 包**）：

1. **单栏实时渲染编辑器（纯 WPF 自研，零新增依赖）**：类 Typora/Notion——输入 `# `/`## `/`### `/`- `/`- [ ] ` 实时变为标题/列表/任务块并吃掉标记，`**文字**` 渲染加粗；无双栏、无编辑/预览切换。
2. **交互式任务列表**：`- [ ] ` 即变带复选框的任务块（与任务列表同款强调色圆环勾选框）。
3. **便签 ↔ 待办双向流转**：每个任务块持稳定 block_id；切到 MD 时**自动把当前待办带入便签**（未链接的追加为任务块，已有 MD 文本保留）；切回待办时**自动**把新任务块转为 TodoItem 并建立链接（已勾选的直接进"已完成"、不放烟花）；便签内勾选 ↔ 全局待办完成状态双向同步；待办被删后链接自愈、可重新转换；**MD 文本永远常驻、不被清空**。
4. **格式工具栏**：加粗（Ctrl+B）/标题循环/无序列表/待办列表，按钮不夺焦点。
5. **多便签管理**：顶部下拉切换（标题取首行）、新建/删除（二次确认）；编辑停顿 0.8s 自动保存，勾选/切换/退出立即落盘。
6. **视图切换**：任务区头部（排序/日历按钮左侧）新增 MD↔待办切换按钮，便签视图右上角「返回待办」切回（同批反馈：取消侧栏入口与独立"提取待办"按钮，切换即转换）；统一进场动画、重启恢复上次视图；与右侧日历面板可共存。
7. **中文 IME 防护**：组合输入期间挂起 Markdown 触发器，整句中文输入不被打断。
8. **国际化**：新增 `S.Note.*` 13 键中英双语；**数据兼容**：`AppData` 仅新增 3 个带默认值字段，旧 data.json 完全兼容。

> 提示词原要求 Tiptap/Vue/Pinia（Web 栈），经确认改为纯 WPF 自研以保住「无依赖单文件 exe」约束。详细实现见 `优化记录.md` 第三十七轮。

### v1.2.0 补3（第三十八轮）—— 2026-06-10（版本号保持 1.2.0）

便签模块重构（**本轮仅构建无依赖 exe，暂不发布 Release 包**）：

1. **编辑器重写为 RichTextBox 富文本**：原"块列表"每行是独立控件，先天无法跨行拖选、回车切焦点脆弱（出现不可输入的虚线框）。改用单一 RichTextBox：原生跨行/跨段选择、复制粘贴、撤销；正文以 Markdown 字符串持久化（`Note.Content`），打开时解析为 FlowDocument、编辑后序列化回 Markdown。
2. **格式**：工具栏 B/I/U/S + Ctrl+B/I/U，新增**斜体、下划线、删除线**；标题(H1→H2→H3 循环)/无序列表/可勾选任务项。放弃"输入 # 即时变标题"的语法实时转换（改为工具栏/快捷键）。
3. **取消待办↔便签联动**：移除双向同步、提取待办、头部便签切换按钮。
4. **侧栏重排**：所有待办 / 普通分组 / **+ 新建待办分组（原地内联输入、移到分组之后）** / 已完成（独立成行）/ **收集箱（右键新建便签）/ + 写点什么 / 便签列表**。便签由侧栏点击打开、右键删除。
5. **数据兼容**：旧"块格式"便签加载时一次性迁移为 Markdown 正文；`AppData` 字段不变。**国际化**：增删 `S.Note.*` 键，中英成对。

> 详细实现见 `优化记录.md` 第三十八轮·补3。

## v1.1.7（第三十六轮）—— 2026-06-10

版本号升至 **1.1.7**（`<Version>1.1.7` / `<FileVersion>1.1.7.0` / `<AssemblyVersion>1.1.7.0`）。界面现代化 + iOS/macOS 风格灵动动画（共三批改动，2026-06-10 经 `release.ps1` **正式发布 Release**）：

1. **统一动画规范**：新增 `Anim` 动画工具（Fast 120ms / Base 200ms / Emphasis 280ms 弹簧过冲 / Bounce 弹跳），全部动画跟随 Windows 系统"显示动画"无障碍开关。
2. **按钮按压回弹**：主按钮/幽灵按钮/图标按钮按下轻缩、松开弹簧回弹；hover 由瞬时变色改为平滑渐变。
3. **勾选对勾弹跳**：完成勾选时对勾 0→1.18→1 过冲回弹 + 淡入，与完成滑出动画衔接（先弹勾再飞走）。
4. **任务卡片浮起**：hover 平滑渐变 + 轻阴影"浮起"；卡片/输入框内边距微调更透气（12,8→14,9 / 10,7→11,8）。
5. **弹窗进场动画**：设置/编辑任务/图标选择/帮助/更新/主题选择/主题编辑 7 个弹窗打开时缩放 0.96→1（弹簧）+ 淡入。
6. **新任务出现动画**：添加待办后新卡片淡入 + 弹性上移。
7. **托盘恢复淡入 / 日程面板弹性滑入 / Toast 弹簧上滑**。
8. **行距 bug 修复**：设置滑块拖到 0.4 不再被 FontManager 夹回 0.8；非法值兜底对齐产品默认 13/1.0。
9. **全主题适配**：所有外观/动画改动仅使用既有 18 个主题颜色键，33+ 套内置主题与自定义主题自动适配；`AppData` 无字段变更、零新增文案（中英文资源无改动）。

补充（同版本第二批，2026-06-10）：

10. **弹层动画全面铺开**：右键菜单 / ComboBox 下拉 / 日期日历弹层每次打开播放与设置弹窗同款"缩放 0.95→1 弹簧 + 淡入"；ToolTip 快速淡入；主窗口 5 个内嵌 Popup 加原生 Fade。
11. **换主题整窗交叉淡变**：点主题色板时旧皮肤截图盖层 260ms 淡出，丝滑过渡不再瞬间跳变（编辑器逐色预览不触发；系统关动画时直达终态）。
12. **拖拽待办视觉**：拖动时半透明卡片影像跟随光标，插入指示线改主题强调色（此前默认灰几乎看不见）。
13. **UI 现代化**：侧栏选中项左侧强调色指示条（弹性展开）；任务区标题 17px Bold；倒计时改胶囊徽章；滚动条 10→8px。

补充（同版本第三批，2026-06-10）：

14. **修复侧栏指示条残留**：切到其他分组后旧分组的强调色指示条不再保持高亮（补退场动画压掉动画保持值）。
15. **日历动画灵动化**：切天/周/月与回今天播设置弹窗同款缩放+淡入；上/下翻页内容沿翻页方向弹性滑入；后台自动刷新不闪动画。
16. **默认排版优化**：默认字体改微软雅黑 UI（+Segoe UI 回退）、字号 13→14、行距 1.0→1.1（仅新安装/恢复默认生效，老用户存值不变；中英文恢复默认文案同步）。
17. **hover 渐变铺开**：标题栏按钮、关闭红钮、交通灯、排序/设置切换钮、侧栏功能按钮、右键菜单项、下拉项、日历分段标签——hover 全部由瞬时变色改为平滑渐变。

> 详细实现见 `优化记录.md` 第三十六轮、补、补2。

## v1.1.6（第三十五轮）—— 2026-06-08

版本号升至 **1.1.6**（`<Version>1.1.6` / `<FileVersion>1.1.6.0` / `<AssemblyVersion>1.1.6.0`）。8 项体验反馈修复（**本轮仅构建无依赖 exe，暂不发布 Release 包**）：

1. **日历多任务并排**：天/周视图同时段任务按碰撞分簇平分列宽并排、不再重叠；月视图改为横向并排紧凑色块（自动换行 + `+N`）。
2. **日期选择器全皮肤适配**：新增 `CalendarDayButton`/`CalendarButton` 主题化模板，所有深色/彩色皮肤下日号、今天、选中、非本月均清晰可辨。
3. **拖任务到日历设截止时间**：从待办列表拖任务到日历即设截止时间——天/周到落点对应整点、月到当天 18:00；首次打开日历弹一次功能提示（仅一次）。
4. **背景调柔**：马卡龙（薄荷/樱花/柠檬）与敦煌（石青/土红）等鲜艳浅色主题整体降饱和、更耐久看。
5. **行距下限放宽**：设置里行距最小值由 0.8 降到 0.4，可更紧凑。
6. **默认排版优化**：默认字号 13、行距 1.0、勾选框 18（更舒适，仅影响新安装/恢复默认）。
7. **国内节假日**：日历联网显示国内法定节假日（标注节日名 + 放假日高亮），设置中可开关、默认开启。
8. **关于页改为「支持开发者」**：邀请到 GitHub 给项目点 Star，点击打开仓库。

> 详细实现见 `优化记录.md` 第三十五轮。

## v1.1.5（重新发布·第三十四轮）—— 2026-06-07

版本号**保持 1.1.5**（`<Version>1.1.5` / `<FileVersion>1.1.5.0` / `<AssemblyVersion>1.1.5.0` 未变），删除原 `v1.1.5` tag 与 Release 后用本轮构建覆盖重发资产。6 项体验反馈修复：

1. **托盘显示主界面取消贴边并居中**：贴边隐藏时点托盘「显示」先解除贴边，再居中到光标所在屏。
2. **日历缩放只缩日历**：开日历后拖窗口外边缘只缩放日历、待办宽度不变；待办宽度仅由中间分隔条调整。
3. **主题收藏分组**：任意主题右键可加入/取消收藏，「收藏」分组**置于最前**（在「常用」之上），可拖动排序（持久化 `AppData.FavoriteThemeKeys`）；分组始终显示，为空时展示「右键收藏」提示文字。
7. **使用说明刷新**：应用内「使用说明」补全历史+本轮全部功能（独立主题窗口/收藏/实时预览/日历缩放/托盘居中/自动更新）。
4. **自定义主题日历兼容**：自定义主题的日历相关辅助色由已编辑色派生，今日高亮/悬停/次要文字随主题协调。
5. **自定义主题实时预览**：编辑改色即时换肤（含日历），未保存关闭还原旧主题，保存才持久化。
6. **圆角排查**：审计全部弹窗均已透明圆角；顶层右键菜单 Border 增加阴影留白边距。

> 注意：版本号未变，已在 1.1.5 的用户不会被自动更新提示到本轮修复。详细实现见 `优化记录.md` 第三十四轮。

## v1.1.5 —— 2026-06-07

主题系统大改 + 反馈修复（正式发布）。版本号同步：`<Version>1.1.5` / `<FileVersion>1.1.5.0` / `<AssemblyVersion>1.1.5.0`。本版正式发布下面标注为「v1.1.4 本地构建·待发布」的全部改动（第三十二、三十三轮）：

- **独立主题窗口**：可拖动、可调整大小、停靠主程序侧边、单击实时预览。
- **分组配色（约 90 套）**：常用 / 经典 / 莫兰迪 / 马卡龙 / 敦煌 / 蒙德里安 / 孟菲斯 / 洛可可 / 马蒂斯 / 透明，命名组各 12 套；“常用”按上次使用排序。
- **分组快速选择栏**：点击直接跳转到分组（非下拉）。
- **自定义主题新建 / 编辑 / 删除**：右键菜单 + 内置 HSV 取色器（不再手输颜色）。
- **可读性**：全配色对比度校验，含右键菜单与日历；切主题日历实时重渲染（修深色日历发暗）。
- **圆角**：修复右键菜单直角；DatePicker / Calendar 主题化并圆角。
- 修坑：`ThemeManager.BuildFromColors` 补齐 `PopupBg`；撤销致直角的 `HasDropShadow=False`。
- 数据兼容：仅新增 `AppData.ThemeUsageOrder`（默认空），向后兼容。

详细实现见 `优化记录.md` 第三十二、三十三轮。

## v1.1.4 —— 2026-06-04

界面一致性 / 可读性修复 + 新增 12 套配色主题（对应需求 1–13）。

- 体积（需求1）：WPF 自包含单文件无法安全裁剪，按"无依赖"约束走极保守优化，体积约 ~60MB。
- 子待办计数即时刷新；"所有待办"显示已完成子待办，与普通分组一致（需求2）。
- 右键菜单去除左侧白色矩形（自定义无 gutter 模板）（需求3）。
- 左侧分组层级缩进：普通分组缩进为"所有待办"子级（需求4）。
- 精简任务右键菜单：移除 清除截止日期 / 折叠展开 / 切换周期提醒 / 标记完成（需求5）。
- 支持拖动任务到左侧分组移动归属（需求6）。
- 编辑任务弹窗可拖动（需求7）。
- 所有窗体/弹层圆角统一（补充圆角 ToolTip / ComboBox）（需求8）。
- 1080p 清晰度：Per-Monitor V2 DPI 清单 + 像素对齐文本渲染（需求9）。
- 缩小添加按钮（需求10）。
- 设置新增"关于"分组：版本号 + 检查更新 + GitHub 链接（需求11）。
- 新增 12 套主题：莫兰迪×3 / 马卡龙×3 / 敦煌×3 / 蒙德里安×3（需求12）。
- 各主题弹窗/预览文字对比度校验，深色/纯黑下可读（需求13）。

版本号同步：`<Version>1.1.4` / `<FileVersion>1.1.4.0` / `<AssemblyVersion>1.1.4.0`。

### 主题窗口反馈修复（v1.1.4，2026-06-07，本地构建·待发布）

> 版本号保持 **1.1.4** 不变。基于上一轮实测反馈的 6 项修复；详见 `优化记录.md` 第三十三轮。

- **右键菜单直角**：撤销上一轮 `HasDropShadow=False`（它会关掉弹窗透明导致四角被填成方角）。
- **深色主题日历可读性**：根因是日历用代码画刷快照渲染、切主题不重绘；新增 `ThemeManager.ThemeChanged` 事件让日历随主题实时重渲染，并提升星期表头对比度。
- **自定义主题可编辑/删除**：色板右键菜单「编辑/删除」；编辑器新增编辑模式（保留 Key 与未编辑颜色）。
- **主题窗口可调整大小**：`WindowChrome` 可 resize + 圆角裁剪随尺寸更新；色板随窗口拉宽显示更多。
- **分组快速选择栏**：顶部分组 chips，点击直接滚动到对应分组（非下拉框）。
- **自定义主题改“选颜色”**：新增纯 WPF 的 HSV 取色器（色盘 + 色相滑条 + 十六进制），替换手输颜色。

### 主题独立窗口 + 分组扩充 + 圆角修复（v1.1.4，2026-06-07，本地构建·待发布）

> 版本号保持 **1.1.4**（`<Version>` / `<FileVersion>` / `<AssemblyVersion>` 不变）；本次仅推送代码与本地无依赖 exe，暂不发 Release。详细实现见 `优化记录.md` 第三十二轮。

- **修改主题改为独立窗口**：新增 `ThemePickerWindow`，可拖动、定位主程序侧边不遮挡、单击主题即实时预览并持久化；「新建自定义主题」改为非模态。
- **主题分组 + 每组补满 12 套**：常用（当前 + 最近使用 9 个，按上次使用排序）、经典、莫兰迪、马卡龙、敦煌、蒙德里安、孟菲斯、洛可可、马蒂斯（命名组各 12 套）、透明（透明/毛玻璃 + 磨砂×2 + 薄雾）。配色按对比度锚定派生，深色主题下右键菜单与日历均可读。
- **圆角修复**：关闭 `ContextMenu`/`ToolTip` 的系统方角投影（右键菜单不再露直角）；主题化并圆角化系统 `DatePicker`/`Calendar`。
- **修坑**：`ThemeManager.BuildFromColors` 补齐 `PopupBg`，修复调色板/自定义主题弹窗背景串色。
- 数据兼容：仅新增 `AppData.ThemeUsageOrder`（默认空），向后兼容。

### 补丁（同 1.1.4 / 1.1.3 资产，2026-06-04）

- 修复"检查更新"在 GitHub 匿名接口限流(403)等失败时误报"已是最新"：`CheckAsync` 失败改抛异常，手动检查显示"检查失败，请稍后重试"。
- 检查频率由每 1 小时放宽到**启动后一次 + 每 12 小时一次**，降低撞限流概率。
- 以修复后的构建替换 **v1.1.4** 与 **v1.1.3** 两个 Release 的 exe 资产（版本号分别保持 1.1.4 / 1.1.3）。

### 补丁二（同 1.1.4 / 1.1.3 资产，2026-06-04）

- 更新对话框的发布说明**跟随用户界面语言**只显示对应语种（中/英），不再中英混排。
- 发布说明把原始 Markdown 渲染为**可读文本**（去掉 `#`、`**`、列表记号等）。
- 修复**点更新后旧版退出、新版未启动**：更新重启(带 `--updated-from`)时单实例静默接管不弹框、接管超时也继续运行；启动新版改为带重试 + 指定工作目录，规避新 exe 刚落地被占用导致拉起失败。

### 补丁三（同 1.1.4 / 1.1.3 资产，2026-06-04）

- **彻底修复「点更新后新版在部分电脑上仍不启动」**：根因是上一版的重启依赖隐藏 PowerShell 临时脚本，在受控/企业机器（组策略把 ExecutionPolicy 设在 MachinePolicy 作用域使 `-Bypass` 失效、AppLocker/杀软拦截 `%TEMP%` 脚本、Constrained Language Mode）会**静默失败**，而旧版此时已退出，用户毫无察觉。
  - 改为**直接 `Process.Start`（`UseShellExecute=true`）拉起新版**，不再依赖 PowerShell/cmd 脚本（`UpdateService.TryStartNewVersion`）。
  - 新增**「新版已启动」命名事件握手**：旧版拉起新版后等待确认，新版启动即 Set（`SignalUpdatedStarted`）。
  - **兜底提醒**：若多次尝试仍未确认新版启动，旧版**保持存活**并弹出对话框，显示新版完整路径与「打开所在文件夹 / 立即运行 / 关闭」，引导用户手动运行。
- 以修复后的构建替换 **v1.1.4** 与 **v1.1.3** 两个 Release 的 exe 资产（版本号分别保持 1.1.4 / 1.1.3）。

## v1.1.3 —— 2026-05（自动更新）

- 新增基于 GitHub 的自动更新：启动 + 每小时检查、一键下载替换、可关闭 / 忽略 / 此版本不再提示。
- 发布范围调整：仅 v1.1.3 起携带自动更新；旧版（v1.0.0–v1.1.2）还原为原版无自动更新。

## v1.1.2 及更早

- 早期功能与体验迭代详见 `优化记录.md`（第一轮起至第二十九轮）。
